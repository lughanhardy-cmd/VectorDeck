{
  "name": "vectordeck",
  "version": "0.1.0",
  "private": true,
  "description": "Virtual airline route planner and briefing beta for MSFS and X-Plane.",
  "scripts": {
    "start": "node server.js",
    "dev": "PORT=4188 node server.js",
    "build:data": "node build-route-data.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=18"
  }
}
